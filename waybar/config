{
    //-*- mode: jsonc -*-
    "layer": "top",
    "position": "top",
    "height": 32,
    "modules-left": ["hyprland/workspaces"],
    "modules-center": ["network", "clock", "pulseaudio"],
    "modules-right": ["tray", "battery", "custom/wp-button"],

    "hyprland/workspaces": {
        "on-click": "activate"
    },

    "clock": {
        "format": "{:%a %b %d  %H:%M:%S}",
        "interval": 1
    },

    "pulseaudio": {
        "format": "{volume}% {icon}",
        "format-muted": " Muted",
        "format-icons": ["", ""],
        "scroll-step": 5,
        "on-click": "pavucontrol-qt"
    },

    "network": {
        "format-wifi": " {essid}",
        "format-ethernet": " Wired",
        "format-disconnected": " Disconnected",
        "on-click": "nm-connection-editor"
    },

    "battery": {
        "format": "{capacity}% {icon}",
        "format-icons": ["", "", "", "", ""]
    },

    "custom/wp-button": {
        "format": "🪄",
        "tooltip": "Change wallpaper and theme",
        "on-click": "systemctl --user start pick-colors.service",
        "interval": 0
    },

    "tray": {
        "spacing": 10
    }
}
